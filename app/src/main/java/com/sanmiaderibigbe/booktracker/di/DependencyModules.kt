package com.sanmiaderibigbe.booktracker.di

import org.koin.dsl.module.module

object DependencyModules {
    val appModules = module {

//        factory { AppDatabase.getDatabase(get(), false )}
//
//        single { BookDao_Impl( get()) as BookDao }
//        //single { Book() }
    }
}